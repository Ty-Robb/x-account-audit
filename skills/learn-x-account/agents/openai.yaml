interface:
  display_name: "X Account Learning Loop"
  short_description: "Find what earns qualified engagement on X"
  default_prompt: "Use $learn-x-account to turn my early X results into an owned first-sprint learning plan."
policy:
  allow_implicit_invocation: false
