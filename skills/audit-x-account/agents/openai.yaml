interface:
  display_name: "X Account Audit"
  short_description: "Audit X accounts with reports and action plans"
  default_prompt: "Use $audit-x-account to audit an X account and produce a complete evidence-led report with an execution-ready first sprint."
policy:
  allow_implicit_invocation: false
